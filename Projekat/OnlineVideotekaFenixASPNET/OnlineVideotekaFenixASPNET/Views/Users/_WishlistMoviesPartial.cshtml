@using OnlineVideotekaFenixASPNET.Models
@model WishlistMoviesViewModel
@{
    ViewBag.Title = "Wishlist";
}


<fieldset>
   <legend>Wishlist</legend>
   
    
    <table class="table table-bordered table-hover">
        <thead>
        <tr>
            <th>Movie</th>
        </tr>
        </thead>
        <tbody>
        @foreach (var movie in Model.WishList)
        {
            <tr>
                <td>@Html.ActionLink(movie.NazivFilma, "FilmOverview", "Movies", new { id = movie.Id }, null)</td>
            </tr>
        }
        </tbody>
    </table>


   
    <table>
        <tr>
            <td><div1>
                @Html.TextBoxFor(m => m.NewMovie, new {@class = "form-control"})
                @Html.ValidationMessageFor(m => m.NewMovie, "", new {@class = "text-danger"})
            </div1></td>
            <td><div2><input type="submit" value="Add" onclick="location.href = '@Url.Action("AddMovieWishList", "Users")'" class="btn btn-primary col-md-offset-4" /></div2></td>
        </tr>
    </table>
</fieldset>
